<div class="container">
  <h1>{{ collection.title }}</h1>
  <div class="products-grid">
    {% for product in collection.products %}
      {% render 'product-card', id: product.id, title: product.title, price: product.price, emoji: '🛍️' %}
    {% endfor %}
  </div>
</div>
